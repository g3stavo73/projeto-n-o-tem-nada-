{
  "name": "@workspace/sarah",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd config && vite --host 0.0.0.0",
    "build": "cd config && vite build",
    "build:pages": "cd config && BASE_PATH=/ vite build",
    "serve": "cd config && vite preview --host 0.0.0.0",
    "typecheck": "cd config && tsc -p tsconfig.json --noEmit"
  }
}
